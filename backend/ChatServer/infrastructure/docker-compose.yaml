services:
  cassandra:
    image: cassandra:5.0
    container_name: canto-cassandra
    ports:
      - "9042:9042"
    environment:
      CASSANDRA_CLUSTER_NAME: canto-cluster
    volumes:
      - cassandra_data:/var/lib/cassandra

volumes:
  cassandra_data: